---
base: 'ubuntu/trusty'

apt:
  packages:
    - git

python27:
  packages:
    - pymysql==0.6
  requirements:
    - requirements.txt

npm:
  packages:
    - grunt-cli 

after_install: 
  - echo "yo, kill me please"
