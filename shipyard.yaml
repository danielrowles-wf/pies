---
base: 'ubuntu/trusty'

apt:
  packages:
    - git

python27:
  packages:
    - boto  
    - pbr
  requirements:
    - requirements.txt

npm:
  packages:
    - grunt-cli 

after_install: 
  - echo "yo, kill me please"
