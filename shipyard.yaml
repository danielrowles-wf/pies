---
base: 'ubuntu/trusty'

apt:
  packages:
    - apt-transport-https
    - git
    - nginx
    - supervisor
    - unzip
    - wget
    - tar

python27:
  packages:
    - pymysql==0.6

after_install:
    - echo "yo, kill me please"
