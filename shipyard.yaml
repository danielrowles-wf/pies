---
base: 'ubuntu/trusty'

apt:
  packages:
    - apt-transport-https
    - git
    - nginx
    - supervisor
    - unzip
    - wget
    - tar

python27:
  packages:
    - pymysql==0.6
  requirements:
    - requirements.txt

npm:
  packages:
    - grunt-cli
    - w-auth-proxy

after_install: 
  - echo "yo, kill me please"
