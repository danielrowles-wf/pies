---
base: 'ubuntu/xenial'

apt:
  packages:
    - apt-transport-https
    - git
    - nginx
    - supervisor
    - unzip
    - wget
    - tar

python27:
  packages:
    - pymysql==0.6

after_install:
    - cat /etc/*release
    - echo "yo, kill me please"
