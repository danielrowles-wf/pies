---
base: 'ubuntu/xenial'

apt:
  packages:
    - apt-transport-https
    - git
    - nginx
    - supervisor
    - unzip
    - wget
    - tar

python27:
  packages:
    - pymysql==0.6

after_install:
    - lsb_release -a
    - echo "yo, kill me please"
