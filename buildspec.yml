version: 0.1

phases:
  pre_build:
    commands:
      - echo Pre-build...
  build:
    commands:
      - echo Build started on `date` 
  post_build:
    commands:
      - echo Build completed on `date` 
